Rubyで数値を切り捨てるためのメソッド
# 基本的な使い方
number = 3.7
puts number.floor  # => 3

以下の場合は、3.7 は小数点以下を切り捨てて 3 になる。
number = -2.3
puts number.floor  # => -3

# floor メソッドに小数点以下の桁数を指定して、特定の桁数で切り捨てることもできる。
number = 123.456
puts number.floor(2)  # => 123.45
puts number.floor(1)  # => 123.4

